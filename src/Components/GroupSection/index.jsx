const GroupCardSection = ({ variant, children }) => {
  const group_members = "";
  const goal = "";
  const event = "";

  const headerInfo = {
    membros: { text: "Membros", img: group_members },
    metas: { text: "Metas", img: goal },
    atividades: { text: "Atividades", img: event },
  };

  return <></>;
};

export default GroupCardSection;
