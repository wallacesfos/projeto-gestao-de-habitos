// colocar o token aqui
const token = "";

// Inserir as chaves do body
const body = {};

// inserir aqui os argumentos que a função pedir - caso peça um objeto
const arg = {};

export const teste = () => {
  // importar e chamar a função aqui dentro exemplo:
  // login(body)
  //   .then((resp) => console.log(resp))
  //   .catch((err) => console.log(err));
};
